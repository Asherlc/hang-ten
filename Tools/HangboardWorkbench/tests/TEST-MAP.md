# Workbench frontend integration test map

This map audits every top-level test function in the three mature Workbench suites at commit `3a95c976`. Pairing is by preserved declaration order within each restored suite. Schema-v2-only representations are not retained: each such row names its schema-v3 behavioral replacement.

- react-app.test.tsx: 38 prior functions → restored-app.test.tsx: 38 v3 replacements
- react-editor.test.tsx: 130 prior functions → restored-editor.test.tsx: 130 v3 replacements (+ 1 v3 rollback regression)
- workbench-modules.test.ts: 40 prior functions → restored-modules.test.ts: 40 v3 replacements (+ 1 dangling-reference regression)

Total: **208 prior functions → 208 schema-v3 replacements**, plus 2 additional v3 regression functions. The independent historical path-editor suite (81 top-level functions) was audited in place and remains outside this three-suite map; its preset, quadratic-removal, and cubic-removal tests now validate their output inside complete native-v3 documents.

Category totals across the 208 mapped replacements: Contact facts/media 43; Path/pointer 51; Persistence/client 38; Git/auth 21; Touch/zoom 16; App/editor integration 11; Constraints 10; History/replacement 8; Guides 6; Presentation/model 4.

| # | Prior suite/function | Schema-v3 replacement | Category | Disposition |
|---:|---|---|---|---|
| 1 | `react-app.test.tsx:231` — renderReact restores browser globals when the initial render throws | `restored-app.test.tsx:245` — renderReact restores browser globals when the initial render throws | App/editor integration | Same behavior, ported to native schema-v3 document fixtures and assertions. |
| 2 | `react-app.test.tsx:242` — the React shell preserves the direct-workbench DOM and renders logged-out auth safely | `restored-app.test.tsx:256` — the React shell preserves the direct-workbench DOM and renders logged-out auth safely | Git/auth | Same behavior, ported to native schema-v3 document fixtures and assertions. |
| 3 | `react-app.test.tsx:277` — the board list shows an accessible attention status only for flagged boards | `restored-app.test.tsx:291` — the board list shows an accessible attention status only for flagged boards | App/editor integration | Same behavior, ported to native schema-v3 document fixtures and assertions. |
| 4 | `react-app.test.tsx:307` — the board list marks model-only packages read-only and cannot open them | `restored-app.test.tsx:321` — the board list marks model-only packages read-only and cannot open them | Presentation/model | Same behavior, ported to native schema-v3 document fixtures and assertions. |
| 5 | `react-app.test.tsx:339` — mobile canvas controls open the board drawer, repository sheet, and hold inspector explicitly | `restored-app.test.tsx:353` — mobile canvas controls open the board drawer, repository sheet, and contact inspector explicitly | Git/auth | Renamed to contact-first terminology while preserving the interaction or contract behavior. |
| 6 | `react-app.test.tsx:382` — canvas background selectors share the viewport background | `restored-app.test.tsx:398` — canvas background selectors share the viewport background | App/editor integration | Same behavior, ported to native schema-v3 document fixtures and assertions. |
| 7 | `react-app.test.tsx:402` — collapsing the mobile hold sheet retains the selected hold | `restored-app.test.tsx:418` — collapsing the mobile contact sheet retains the selected contact | Contact facts/media | Renamed to contact-first terminology while preserving the interaction or contract behavior. |
| 8 | `react-app.test.tsx:423` — selecting a hold on another board does not reopen an explicitly opened mobile sheet | `restored-app.test.tsx:439` — selecting a contact on another board does not reopen an explicitly opened mobile sheet | Contact facts/media | Renamed to contact-first terminology while preserving the interaction or contract behavior. |
| 9 | `react-app.test.tsx:453` — autosave restores the saved browser preference | `restored-app.test.tsx:469` — autosave restores the saved browser preference | Persistence/client | Same behavior, ported to native schema-v3 document fixtures and assertions. |
| 10 | `react-app.test.tsx:463` — changing autosave persists the preference for a future app mount | `restored-app.test.tsx:479` — changing autosave persists the preference for a future app mount | Persistence/client | Same behavior, ported to native schema-v3 document fixtures and assertions. |
| 11 | `react-app.test.tsx:480` — manual board refresh does not masquerade as completed repository initialization | `restored-app.test.tsx:496` — manual board refresh does not masquerade as completed repository initialization | Git/auth | Same behavior, ported to native schema-v3 document fixtures and assertions. |
| 12 | `react-app.test.tsx:494` — state-dependent actions observe updates dispatched earlier in the same task | `restored-app.test.tsx:510` — state-dependent actions observe updates dispatched earlier in the same task | App/editor integration | Same behavior, ported to native schema-v3 document fixtures and assertions. |
| 13 | `react-app.test.tsx:514` — API errors render angle-bracket text without interpreting an image node | `restored-app.test.tsx:530` — API errors render angle-bracket text without interpreting an image node | App/editor integration | Same behavior, ported to native schema-v3 document fixtures and assertions. |
| 14 | `react-app.test.tsx:528` — startup API failures appear in the dedicated editor error alert | `restored-app.test.tsx:544` — startup API failures appear in the dedicated editor error alert | App/editor integration | Same behavior, ported to native schema-v3 document fixtures and assertions. |
| 15 | `react-app.test.tsx:544` — a failed save preserves its API message in the editor error alert | `restored-app.test.tsx:560` — a failed save preserves its API message in the editor error alert | Persistence/client | Same behavior, ported to native schema-v3 document fixtures and assertions. |
| 16 | `react-app.test.tsx:563` — successful API startup does not show an editor API error | `restored-app.test.tsx:579` — successful API startup does not show an editor API error | App/editor integration | Same behavior, ported to native schema-v3 document fixtures and assertions. |
| 17 | `react-app.test.tsx:573` — initialization awaits authentication, repository status, then boards | `restored-app.test.tsx:589` — initialization awaits authentication, repository status, then boards | Git/auth | Same behavior, ported to native schema-v3 document fixtures and assertions. |
| 18 | `react-app.test.tsx:597` — board selection locks board and Git actions and commits image plus document together | `restored-app.test.tsx:613` — board selection locks board and Git actions and commits image plus document together | Git/auth | Same behavior, ported to native schema-v3 document fixtures and assertions. |
| 19 | `react-app.test.tsx:627` — selecting a listed board starts its image request before board details settle | `restored-app.test.tsx:643` — selecting a listed board starts its image request before board details settle | Persistence/client | Same behavior, ported to native schema-v3 document fixtures and assertions. |
| 20 | `react-app.test.tsx:660` — failed board selection keeps the prior editor and failed save keeps unsaved edits | `restored-app.test.tsx:676` — failed board selection keeps the prior editor and failed save keeps unsaved edits | Persistence/client | Same behavior, ported to native schema-v3 document fixtures and assertions. |
| 21 | `react-app.test.tsx:702` — recoverable save authentication failure keeps edits and offers safe separate-tab login | `restored-app.test.tsx:718` — recoverable save authentication failure keeps edits and offers safe separate-tab login | Git/auth | Same behavior, ported to native schema-v3 document fixtures and assertions. |
| 22 | `react-app.test.tsx:752` — selecting another board clears save authentication recovery state | `restored-app.test.tsx:768` — selecting another board clears save authentication recovery state | Git/auth | Same behavior, ported to native schema-v3 document fixtures and assertions. |
| 23 | `react-app.test.tsx:793` — branch changes clear save authentication recovery with the editor | `restored-app.test.tsx:809` — branch changes clear save authentication recovery with the editor | Git/auth | Same behavior, ported to native schema-v3 document fixtures and assertions. |
| 24 | `react-app.test.tsx:837` — an old delayed save cannot overwrite a newer document identity | `restored-app.test.tsx:853` — an old delayed save cannot overwrite a newer document identity | Persistence/client | Same behavior, ported to native schema-v3 document fixtures and assertions. |
| 25 | `react-app.test.tsx:866` — autosave waits for a quiet 750ms window and saves the latest valid document once | `restored-app.test.tsx:882` — autosave waits for a quiet 750ms window and saves the latest valid document once | Persistence/client | Same behavior, ported to native schema-v3 document fixtures and assertions. |
| 26 | `react-app.test.tsx:905` — a failed autosave waits for another document change before trying again | `restored-app.test.tsx:921` — a failed autosave waits for another document change before trying again | Persistence/client | Same behavior, ported to native schema-v3 document fixtures and assertions. |
| 27 | `react-app.test.tsx:938` — a failed manual save waits for another document change before autosaving | `restored-app.test.tsx:954` — a failed manual save waits for another document change before autosaving | Persistence/client | Same behavior, ported to native schema-v3 document fixtures and assertions. |
| 28 | `react-app.test.tsx:970` — turning autosave off cancels pending work and leaves later edits for manual save | `restored-app.test.tsx:986` — turning autosave off cancels pending work and leaves later edits for manual save | Persistence/client | Same behavior, ported to native schema-v3 document fixtures and assertions. |
| 29 | `react-app.test.tsx:998` — hold editing remains available during a save and survives its stale response | `restored-app.test.tsx:1014` — contact editing remains available during a save and survives its stale response | Persistence/client | Renamed to contact-first terminology while preserving the interaction or contract behavior. |
| 30 | `react-app.test.tsx:1066` — a successful save always reports Board saved after committing the saved document | `restored-app.test.tsx:1084` — a successful save always reports Board saved after committing the saved document | Persistence/client | Same behavior, ported to native schema-v3 document fixtures and assertions. |
| 31 | `react-app.test.tsx:1091` — replacing dependencies starts current Git initialization when obsolete work never settles | `restored-app.test.tsx:1109` — replacing dependencies starts current Git initialization when obsolete work never settles | Git/auth | Same behavior, ported to native schema-v3 document fixtures and assertions. |
| 32 | `react-app.test.tsx:1139` — replacing dependencies releases a save abandoned by the old coordinator | `restored-app.test.tsx:1157` — replacing dependencies releases a save abandoned by the old coordinator | Persistence/client | Same behavior, ported to native schema-v3 document fixtures and assertions. |
| 33 | `react-app.test.tsx:1181` — detached HEAD differs from unavailable status and still permits New Branch | `restored-app.test.tsx:1199` — detached HEAD differs from unavailable status and still permits New Branch | Git/auth | Same behavior, ported to native schema-v3 document fixtures and assertions. |
| 34 | `react-app.test.tsx:1209` — hosted storage omits local commit controls while keeping Save and Open PR | `restored-app.test.tsx:1227` — hosted storage omits local commit controls while keeping Save and Open PR | Git/auth | Same behavior, ported to native schema-v3 document fixtures and assertions. |
| 35 | `react-app.test.tsx:1227` — dirty branch switch and create confirm once and honor cancellation while clean switch does not confirm | `restored-app.test.tsx:1245` — dirty branch switch and create confirm once and honor cancellation while clean switch does not confirm | Git/auth | Same behavior, ported to native schema-v3 document fixtures and assertions. |
| 36 | `react-app.test.tsx:1262` — successful switch and create retain success when status refresh fails and create clears input | `restored-app.test.tsx:1280` — successful switch and create retain success when status refresh fails and create clears input | Git/auth | Same behavior, ported to native schema-v3 document fixtures and assertions. |
| 37 | `react-app.test.tsx:1301` — empty commit never calls the client and push status names the branch | `restored-app.test.tsx:1319` — empty commit never calls the client and push status names the branch | Git/auth | Same behavior, ported to native schema-v3 document fixtures and assertions. |
| 38 | `react-app.test.tsx:1322` — pull request prompts cancel safely and accepted values are trimmed with base main | `restored-app.test.tsx:1340` — pull request prompts cancel safely and accepted values are trimmed with base main | Git/auth | Same behavior, ported to native schema-v3 document fixtures and assertions. |
| 39 | `react-editor.test.tsx:178` — model helpers group physical holds and derive collision-free identifiers | `restored-editor.test.tsx:243` — model helpers group physical contacts and derive collision-free region identifiers | Contact facts/media | Renamed to contact-first terminology while preserving the interaction or contract behavior. |
| 40 | `react-editor.test.tsx:192` — multi-object boards assign new holds and allow deliberate object reassignment | `restored-editor.test.tsx:258` — multi-object boards assign new contacts and allow deliberate object reassignment | Contact facts/media | Renamed to contact-first terminology while preserving the interaction or contract behavior. |
| 41 | `react-editor.test.tsx:216` — metadata warning marks every region of a physical hold missing required metadata | `restored-editor.test.tsx:285` — every raster region must resolve to one factual contact | Contact facts/media | Schema-v2 missing-region-fact warning retired; replaced by strict contact-fact ownership/reference validation or single-owner editing. |
| 42 | `react-editor.test.tsx:230` — metadata warnings require depth only for edges and pockets | `restored-editor.test.tsx:291` — depth is optional for every contact kind and remains fact-owned | Contact facts/media | Schema-v2 missing-region-fact warning retired; replaced by strict contact-fact ownership/reference validation or single-owner editing. |
| 43 | `react-editor.test.tsx:251` — metadata warning updates when an incomplete hold receives its missing field | `restored-editor.test.tsx:321` — editing a contact fact updates its single owner without duplicating it across media pieces | Contact facts/media | Schema-v2 missing-region-fact warning retired; replaced by strict contact-fact ownership/reference validation or single-owner editing. |
| 44 | `react-editor.test.tsx:267` — switching presentations changes the focused canvas and scopes new holds | `restored-editor.test.tsx:338` — switching presentations changes the focused canvas and scopes new contacts | Contact facts/media | Native-v3 focused responses share the complete factual inventory, exact global counts/IDs, and exact per-presentation raster contact IDs; saving a new back contact updates the request and returned global/back inventories without changing front ownership. |
| 45 | `react-editor.test.tsx:357` — deleting the selected surface confirms and focuses the server-selected replacement | `restored-editor.test.tsx:460` — deleting the selected surface confirms and focuses the server-selected replacement | History/replacement | Same behavior, ported to native schema-v3 document fixtures and assertions. |
| 46 | `react-editor.test.tsx:400` — an alias surface explains that it is removed with its canonical source | `restored-editor.test.tsx:503` — an alias surface explains that it is removed with its canonical source | Presentation/model | Same behavior, ported to native schema-v3 document fixtures and assertions. |
| 47 | `react-editor.test.tsx:428` — surface controls are disabled while a board save is in flight | `restored-editor.test.tsx:531` — surface controls are disabled while a board save is in flight | Persistence/client | Same behavior, ported to native schema-v3 document fixtures and assertions. |
| 48 | `react-editor.test.tsx:457` — deleting a surface refreshes its library attention summary | `restored-editor.test.tsx:563` — deleting a surface refreshes its library attention summary | Presentation/model | Same behavior, ported to native schema-v3 document fixtures and assertions. |
| 49 | `react-editor.test.tsx:493` — a delete authentication failure keeps the reauthentication link | `restored-editor.test.tsx:599` — a delete authentication failure keeps the reauthentication link | Git/auth | Same behavior, ported to native schema-v3 document fixtures and assertions. |
| 50 | `react-editor.test.tsx:519` — deleting a surface keeps the server-selected replacement when its image cannot load | `restored-editor.test.tsx:625` — deleting a surface keeps the server-selected replacement when its image cannot load | Persistence/client | Same behavior, ported to native schema-v3 document fixtures and assertions. |
| 51 | `react-editor.test.tsx:572` — rotation handles stay separated and inside both top-edge and narrow canvases | `restored-editor.test.tsx:678` — rotation handles stay separated and inside both top-edge and narrow canvases | Path/pointer | Same behavior, ported to native schema-v3 document fixtures and assertions. |
| 52 | `react-editor.test.tsx:582` — rotation degree normalization accepts decimals and rejects empty, zero, and non-finite values | `restored-editor.test.tsx:688` — rotation degree normalization accepts decimals and rejects empty, zero, and non-finite values | Path/pointer | Same behavior, ported to native schema-v3 document fixtures and assertions. |
| 53 | `react-editor.test.tsx:591` — svgPoint prefers inverse screen CTM and otherwise accounts for meet letterboxing | `restored-editor.test.tsx:697` — svgPoint prefers inverse screen CTM and otherwise accounts for meet letterboxing | App/editor integration | Same behavior, ported to native schema-v3 document fixtures and assertions. |
| 54 | `react-editor.test.tsx:603` — svgPoint preserves finite pointer offsets when fallback geometry has zero dimensions | `restored-editor.test.tsx:709` — svgPoint preserves finite pointer offsets when fallback geometry has zero dimensions | Path/pointer | Same behavior, ported to native schema-v3 document fixtures and assertions. |
| 55 | `react-editor.test.tsx:613` — selection renders one declarative handle overlay with legacy visual attributes | `restored-editor.test.tsx:719` — selection renders one declarative handle overlay with legacy visual attributes | App/editor integration | Same behavior, ported to native schema-v3 document fixtures and assertions. |
| 56 | `react-editor.test.tsx:625` — canvas zoom controls and Alt-wheel adjust a bounded zoom while preserving normal scrolling | `restored-editor.test.tsx:731` — canvas zoom controls and Alt-wheel adjust a bounded zoom while preserving normal scrolling | Touch/zoom | Same behavior, ported to native schema-v3 document fixtures and assertions. |
| 57 | `react-editor.test.tsx:647` — Command plus and minus shortcuts zoom only an open board outside editable targets | `restored-editor.test.tsx:753` — Command plus and minus shortcuts zoom only an open board outside editable targets | Touch/zoom | Same behavior, ported to native schema-v3 document fixtures and assertions. |
| 58 | `react-editor.test.tsx:686` — Alt-wheel zoom accepts a horizontal-only wheel delta | `restored-editor.test.tsx:792` — Alt-wheel zoom accepts a horizontal-only wheel delta | Touch/zoom | Same behavior, ported to native schema-v3 document fixtures and assertions. |
| 59 | `react-editor.test.tsx:693` — Ctrl-wheel pinch zoom accumulates small deltas without changing Alt-wheel sensitivity | `restored-editor.test.tsx:799` — Ctrl-wheel pinch zoom accumulates small deltas without changing Alt-wheel sensitivity | Touch/zoom | Same behavior, ported to native schema-v3 document fixtures and assertions. |
| 60 | `react-editor.test.tsx:708` — Ctrl-wheel pinch uses 10 percent steps and preserves excess delta for later steps | `restored-editor.test.tsx:814` — Ctrl-wheel pinch uses 10 percent steps and preserves excess delta for later steps | Touch/zoom | Same behavior, ported to native schema-v3 document fixtures and assertions. |
| 61 | `react-editor.test.tsx:718` — standard zoom controls reach bounds from pinch-created zoom levels | `restored-editor.test.tsx:824` — standard zoom controls reach bounds from pinch-created zoom levels | Touch/zoom | Same behavior, ported to native schema-v3 document fixtures and assertions. |
| 62 | `react-editor.test.tsx:734` — Alt-wheel resets pending Ctrl-pinch deltas before its immediate zoom | `restored-editor.test.tsx:840` — Alt-wheel resets pending Ctrl-pinch deltas before its immediate zoom | Touch/zoom | Same behavior, ported to native schema-v3 document fixtures and assertions. |
| 63 | `react-editor.test.tsx:745` — ordinary wheel resets pending Ctrl-pinch deltas before the next pinch | `restored-editor.test.tsx:851` — ordinary wheel resets pending Ctrl-pinch deltas before the next pinch | Touch/zoom | Same behavior, ported to native schema-v3 document fixtures and assertions. |
| 64 | `react-editor.test.tsx:755` — rapid Ctrl-pinch events consume browser zoom while advancing canvas zoom only within bounds | `restored-editor.test.tsx:861` — rapid Ctrl-pinch events consume browser zoom while advancing canvas zoom only within bounds | Touch/zoom | Same behavior, ported to native schema-v3 document fixtures and assertions. |
| 65 | `react-editor.test.tsx:784` — Ctrl-pinch does not consume a sub-threshold event when canvas zoom is at its bound | `restored-editor.test.tsx:890` — Ctrl-pinch does not consume a sub-threshold event when canvas zoom is at its bound | Touch/zoom | Same behavior, ported to native schema-v3 document fixtures and assertions. |
| 66 | `react-editor.test.tsx:794` — wheel zoom uses callbacks from the latest render | `restored-editor.test.tsx:900` — wheel zoom uses callbacks from the latest render | Touch/zoom | Same behavior, ported to native schema-v3 document fixtures and assertions. |
| 67 | `react-editor.test.tsx:849` — Ctrl-pinch retains multiple thresholds when a pinch callback updates zoom without returning a value | `restored-editor.test.tsx:955` — Ctrl-pinch retains multiple thresholds when a pinch callback updates zoom without returning a value | Touch/zoom | Same behavior, ported to native schema-v3 document fixtures and assertions. |
| 68 | `react-editor.test.tsx:885` — two-finger touch pinch continues zooming across rerenders without intercepting one-finger touches | `restored-editor.test.tsx:991` — two-finger touch pinch continues zooming across rerenders without intercepting one-finger touches | Touch/zoom | Same behavior, ported to native schema-v3 document fixtures and assertions. |
| 69 | `react-editor.test.tsx:909` — two-finger touch pinch advances through every 10 percent threshold crossed in one move | `restored-editor.test.tsx:1015` — two-finger touch pinch advances through every 10 percent threshold crossed in one move | Touch/zoom | Same behavior, ported to native schema-v3 document fixtures and assertions. |
| 70 | `react-editor.test.tsx:923` — pointer edits retain SVG coordinates at a zoomed canvas size | `restored-editor.test.tsx:1029` — pointer edits retain SVG coordinates at a zoomed canvas size | Touch/zoom | Same behavior, ported to native schema-v3 document fixtures and assertions. |
| 71 | `react-editor.test.tsx:934` — hold paths expose button semantics and support Enter and Space selection | `restored-editor.test.tsx:1040` — contact paths expose button semantics and support Enter and Space selection | Path/pointer | Renamed to contact-first terminology while preserving the interaction or contract behavior. |
| 72 | `react-editor.test.tsx:948` — modifier selection toggles highlighted holds while plain selection replaces the batch and keeps one primary overlay | `restored-editor.test.tsx:1054` — modifier selection toggles highlighted contacts while plain selection replaces the batch and keeps one primary overlay | Path/pointer | Renamed to contact-first terminology while preserving the interaction or contract behavior. |
| 73 | `react-editor.test.tsx:970` — Command-click selection survives its trailing plain click | `restored-editor.test.tsx:1076` — Command-click selection survives its trailing plain click | Path/pointer | Same behavior, ported to native schema-v3 document fixtures and assertions. |
| 74 | `react-editor.test.tsx:983` — a cancelled Command-click does not suppress the next normal click | `restored-editor.test.tsx:1089` — a cancelled Command-click does not suppress the next normal click | Path/pointer | Same behavior, ported to native schema-v3 document fixtures and assertions. |
| 75 | `react-editor.test.tsx:995` — batch inspector actions change every selected physical hold and undo restores them | `restored-editor.test.tsx:1101` — batch inspector actions change every selected physical contact and undo restores a valid batch deletion | Contact facts/media | Restored with three factual contacts: deleting a selected two-contact subset removes every owned raster piece, and undo restores facts, pieces, and the exact prior selection. Empty-document rollback remains an additional v3 regression. |
| 76 | `react-editor.test.tsx:1045` — batch deletion names every selected physical hold and leaves the document intact when cancelled | `restored-editor.test.tsx:1176` — batch deletion names every selected physical contact and leaves the document intact when cancelled | Contact facts/media | Renamed to contact-first terminology while preserving the interaction or contract behavior. |
| 77 | `react-editor.test.tsx:1063` — the hold type control preserves an out-of-list document value | `restored-editor.test.tsx:1194` — the contact kind control rejects values outside the v3 factual vocabulary | Contact facts/media | Permissive v2 type value retired; replaced by strict v3 contact-kind vocabulary rejection. |
| 78 | `react-editor.test.tsx:1075` — changing exactly two selected physical holds to gastons creates a reciprocal pair that saves | `restored-editor.test.tsx:1209` — changing exactly two selected physical contacts to gastons creates a reciprocal pair that saves | Contact facts/media | Renamed to contact-first terminology while preserving the interaction or contract behavior. |
| 79 | `react-editor.test.tsx:1093` — converting a selected gaston would orphan its unselected counterpart, so it leaves the pair unchanged | `restored-editor.test.tsx:1226` — converting a selected gaston would orphan its unselected counterpart, so it leaves the pair unchanged | Contact facts/media | Same behavior, ported to native schema-v3 document fixtures and assertions. |
| 80 | `react-editor.test.tsx:1118` — converting two holds with the same hold ID to gastons leaves them unchanged with validation | `restored-editor.test.tsx:1258` — converting two pieces of one contact to gastons leaves the fact unchanged with validation | Contact facts/media | Duplicated per-region facts retired; replaced by one factual contact shared by all media pieces. |
| 81 | `react-editor.test.tsx:1139` — an unpaired gaston offers only actionable pairing candidates and saves the initial reciprocal pair | `restored-editor.test.tsx:1282` — an unpaired gaston offers only actionable pairing candidates and saves the initial reciprocal pair | Contact facts/media | Same behavior, ported to native schema-v3 document fixtures and assertions. |
| 82 | `react-editor.test.tsx:1168` — saving an unpaired gaston stays local and explains that it needs a pair | `restored-editor.test.tsx:1320` — saving an unpaired gaston stays local and explains that it needs a pair | Contact facts/media | Same behavior, ported to native schema-v3 document fixtures and assertions. |
| 83 | `react-editor.test.tsx:1182` — an established gaston pair shows its counterpart without offering reassignment or unset | `restored-editor.test.tsx:1335` — an established gaston pair shows its counterpart without offering reassignment or unset | Contact facts/media | Same behavior, ported to native schema-v3 document fixtures and assertions. |
| 84 | `react-editor.test.tsx:1198` — changing a gaston hold to another kind clears its pair metadata | `restored-editor.test.tsx:1358` — changing a gaston contact to another kind clears its pair metadata | Contact facts/media | Restored as an atomic v3 fact edit: both Gaston members take the selected new kind, both pair IDs are removed, and the resulting document validates and saves. |
| 85 | `react-editor.test.tsx:1215` — Add Hold creates and selects a centered square | `restored-editor.test.tsx:1390` — Add contact creates factual ownership and selects a centered square region | Contact facts/media | Renamed to contact-first terminology while preserving the interaction or contract behavior. |
| 86 | `react-editor.test.tsx:1224` — Add segment creates and selects a separately editable piece of the selected hold | `restored-editor.test.tsx:1399` — Add segment creates and selects a separately editable piece of the selected contact | Contact facts/media | Renamed to contact-first terminology while preserving the interaction or contract behavior. |
| 87 | `react-editor.test.tsx:1255` — delete and type changes apply to every piece sharing holdID | `restored-editor.test.tsx:1429` — delete and type changes apply to every piece sharing contactID | Contact facts/media | Renamed to contact-first terminology while preserving the interaction or contract behavior. |
| 88 | `react-editor.test.tsx:1269` — changing a sloper hold to another kind removes sloper-only metadata | `restored-editor.test.tsx:1443` — changing a sloper contact kind preserves independently-owned raster treatment | Contact facts/media | Renamed to contact-first terminology while preserving the interaction or contract behavior. |
| 89 | `react-editor.test.tsx:1293` — duplicate and mirror reflects every selected physical hold with fresh hold identities | `restored-editor.test.tsx:1470` — duplicate and mirror reflects every selected physical contact with fresh contact identities | Contact facts/media | Renamed to contact-first terminology while preserving the interaction or contract behavior. |
| 90 | `react-editor.test.tsx:1341` — duplicate and mirror regenerates bendable indexes from mirrored cubic commands | `restored-editor.test.tsx:1519` — duplicate and mirror regenerates bendable indexes from mirrored cubic commands | Path/pointer | Same behavior, ported to native schema-v3 document fixtures and assertions. |
| 91 | `react-editor.test.tsx:1371` — duplicate and mirror preserves the selected presentation on the new hold | `restored-editor.test.tsx:1553` — duplicate and mirror preserves the selected presentation on the new contact | Contact facts/media | Renamed to contact-first terminology while preserving the interaction or contract behavior. |
| 92 | `react-editor.test.tsx:1410` — duplicate and mirror reserves hold IDs used by other presentations | `restored-editor.test.tsx:1586` — duplicate and mirror reserves contact identities owned by other presentations | Contact facts/media | Full v3 contact inventory includes an unrendered `new-contact-1`; collision is refused and the next exact global identity owns the saved mirrored raster region. |
| 93 | `react-editor.test.tsx:1446` — finger capacity loads in the inspector, applies to every physical piece, and new holds are unset | `restored-editor.test.tsx:1622` — finger capacity loads in the inspector, applies to every physical piece, and new contacts are unset | Contact facts/media | Renamed to contact-first terminology while preserving the interaction or contract behavior. |
| 94 | `react-editor.test.tsx:1474` — depth measurement modes load existing ranges, switch atomically, and save across every physical piece | `restored-editor.test.tsx:1649` — contact depth ranges load, update atomically, and save on factual contacts | Contact facts/media | Renamed to contact-first terminology while preserving the interaction or contract behavior. |
| 95 | `react-editor.test.tsx:1522` — depth measurement modes reopen scalar values and clear the opposite depth form | `restored-editor.test.tsx:1684` — equal contact depth bounds reopen and can become a range or be cleared | Contact facts/media | Renamed to contact-first terminology while preserving the interaction or contract behavior. |
| 96 | `react-editor.test.tsx:1560` — zero depth is visibly invalid in the inspector | `restored-editor.test.tsx:1714` — zero ranged-contact depth stays visibly invalid without replacing the valid factual range | Contact facts/media | Zero remains a visible invalid draft with a positive-depth validation message; it never replaces the valid contact-owned range in the saved v3 document. |
| 97 | `react-editor.test.tsx:1584` — zero fixed depth is visibly invalid in the inspector | `restored-editor.test.tsx:1743` — zero equal-bound contact depth stays visibly invalid without replacing the valid fixed-depth fact | Contact facts/media | Scalar region depth retired; equal contact-owned bounds are the v3 fixed-depth fact, and zero remains a visible invalid draft without entering the saved document. |
| 98 | `react-editor.test.tsx:1598` — clearing an invalid optional depth clears its validation error and saves without a depth range | `restored-editor.test.tsx:1772` — clearing an optional depth saves without a factual range | Persistence/client | Renamed to contact-first terminology while preserving the interaction or contract behavior. |
| 99 | `react-editor.test.tsx:1631` — changing the selected hold clears stale depth validation | `restored-editor.test.tsx:1798` — changing the selected contact replaces the visible factual depth range | Contact facts/media | Renamed to contact-first terminology while preserving the interaction or contract behavior. |
| 100 | `react-editor.test.tsx:1663` — arrows nudge by 1 and 10 while input-targeted arrows retain native behavior | `restored-editor.test.tsx:1821` — arrows nudge by 1 and 10 while input-targeted arrows retain native behavior | Path/pointer | Same behavior, ported to native schema-v3 document fixtures and assertions. |
| 101 | `react-editor.test.tsx:1678` — hand capacity loads in the inspector, applies to every physical piece, and new holds are unset | `restored-editor.test.tsx:1836` — hand capacity loads in the inspector, applies to every physical piece, and new contacts are unset | Contact facts/media | Renamed to contact-first terminology while preserving the interaction or contract behavior. |
| 102 | `react-editor.test.tsx:1706` — command/control undo and redo reverse document edits and preserve native input behavior | `restored-editor.test.tsx:1863` — command/control undo and redo reverse document edits and preserve native input behavior | History/replacement | Same behavior, ported to native schema-v3 document fixtures and assertions. |
| 103 | `react-editor.test.tsx:1731` — a new edit clears redo and a completed drag is a single undo step | `restored-editor.test.tsx:1888` — a new edit clears redo and a completed drag is a single undo step | History/replacement | Same behavior, ported to native schema-v3 document fixtures and assertions. |
| 104 | `react-editor.test.tsx:1743` — undo during a drag restores the committed document before building redo history | `restored-editor.test.tsx:1900` — undo during a drag restores the committed document before building redo history | History/replacement | Same behavior, ported to native schema-v3 document fixtures and assertions. |
| 105 | `react-editor.test.tsx:1762` — command/control undo and Ctrl redo consume browser shortcuts when cancelling an active drag without history | `restored-editor.test.tsx:1919` — command/control undo and Ctrl redo consume browser shortcuts when cancelling an active drag without history | History/replacement | Same behavior, ported to native schema-v3 document fixtures and assertions. |
| 106 | `react-editor.test.tsx:1781` — no-op drags do not create an undo revision | `restored-editor.test.tsx:1938` — no-op drags do not create an undo revision | History/replacement | Same behavior, ported to native schema-v3 document fixtures and assertions. |
| 107 | `react-editor.test.tsx:1797` — escape cancels a drag and command/control save saves only outside editable targets | `restored-editor.test.tsx:1954` — escape cancels a drag and command/control save saves only outside editable targets | Persistence/client | Same behavior, ported to native schema-v3 document fixtures and assertions. |
| 108 | `react-editor.test.tsx:1827` — bracket keys and buttons rotate by 15 and 45 degrees | `restored-editor.test.tsx:1984` — bracket keys and buttons rotate by 15 and 45 degrees | Path/pointer | Same behavior, ported to native schema-v3 document fixtures and assertions. |
| 109 | `react-editor.test.tsx:1851` — arbitrary rotation accepts decimals, normalizes extremes, and rejects invalid values | `restored-editor.test.tsx:2008` — arbitrary rotation accepts decimals, normalizes extremes, and rejects invalid values | Path/pointer | Same behavior, ported to native schema-v3 document fixtures and assertions. |
| 110 | `react-editor.test.tsx:1892` — a freeform anchor keeps its local target ID while it is dragged | `restored-editor.test.tsx:2049` — a freeform anchor keeps its local target ID while it is dragged | Path/pointer | Same behavior, ported to native schema-v3 document fixtures and assertions. |
| 111 | `react-editor.test.tsx:1908` — saving a stable editable path sends no local geometry IDs | `restored-editor.test.tsx:2065` — saving a stable editable path sends no local geometry IDs | Path/pointer | Same behavior, ported to native schema-v3 document fixtures and assertions. |
| 112 | `react-editor.test.tsx:1940` — insertion retains unrelated local target IDs while undo and redo keep canonical paths | `restored-editor.test.tsx:2095` — insertion retains unrelated local target IDs while undo and redo keep canonical paths | History/replacement | Same behavior, ported to native schema-v3 document fixtures and assertions. |
| 113 | `react-editor.test.tsx:1968` — an inserted vertex stays selected through drag and rotation so Delete removes that vertex | `restored-editor.test.tsx:2121` — an inserted vertex stays selected through drag and rotation so Delete removes that vertex | Path/pointer | Same behavior, ported to native schema-v3 document fixtures and assertions. |
| 114 | `react-editor.test.tsx:2007` — deleting then inserting in one edit session never reuses a local vertex ID | `restored-editor.test.tsx:2158` — deleting then inserting in one edit session never reuses a local vertex ID | Path/pointer | Same behavior, ported to native schema-v3 document fixtures and assertions. |
| 115 | `react-editor.test.tsx:2045` — vertex accessibility labels follow current rendered order after insertion | `restored-editor.test.tsx:2194` — vertex accessibility labels follow current rendered order after insertion | Path/pointer | Same behavior, ported to native schema-v3 document fixtures and assertions. |
| 116 | `react-editor.test.tsx:2063` — guide controls create horizontal and vertical guides at the selected hold center | `restored-editor.test.tsx:2210` — guide controls create horizontal and vertical guides at the selected contact center | Guides | Renamed to contact-first terminology while preserving the interaction or contract behavior. |
| 117 | `react-editor.test.tsx:2086` — whole-path dragging snaps its horizontal and vertical bounds edges to nearby guides | `restored-editor.test.tsx:2231` — whole-path dragging snaps its horizontal and vertical bounds edges to nearby guides | Guides | Same behavior, ported to native schema-v3 document fixtures and assertions. |
| 118 | `react-editor.test.tsx:2105` — whole-path dragging does not snap when only its center is near a guide | `restored-editor.test.tsx:2253` — whole-path dragging does not snap when only its center is near a guide | Guides | Same behavior, ported to native schema-v3 document fixtures and assertions. |
| 119 | `react-editor.test.tsx:2122` — Alt bypasses guide snapping during a whole-path drag | `restored-editor.test.tsx:2273` — Alt bypasses guide snapping during a whole-path drag | Guides | Same behavior, ported to native schema-v3 document fixtures and assertions. |
| 120 | `react-editor.test.tsx:2141` — guides drag on their own axis and clear without changing the document | `restored-editor.test.tsx:2295` — guides drag on their own axis and clear without changing the document | Guides | Same behavior, ported to native schema-v3 document fixtures and assertions. |
| 121 | `react-editor.test.tsx:2172` — starting a touch pinch stops an active guide drag | `restored-editor.test.tsx:2324` — starting a touch pinch stops an active guide drag | Touch/zoom | Same behavior, ported to native schema-v3 document fixtures and assertions. |
| 122 | `react-editor.test.tsx:2196` — vertex, control, and whole-path drags derive every move from pointer-down geometry | `restored-editor.test.tsx:2346` — vertex, control, and whole-path drags derive every move from pointer-down geometry | Path/pointer | Same behavior, ported to native schema-v3 document fixtures and assertions. |
| 123 | `react-editor.test.tsx:2215` — rotation drag rotates every sibling from pointer-down paths around the shared centroid | `restored-editor.test.tsx:2366` — rotation drag rotates every sibling from pointer-down paths around the shared centroid | Path/pointer | Same behavior, ported to native schema-v3 document fixtures and assertions. |
| 124 | `react-editor.test.tsx:2233` — rotation drag rotates every Command-selected physical hold around its own centroid | `restored-editor.test.tsx:2384` — rotation drag rotates every Command-selected physical contact around its own centroid | Path/pointer | Renamed to contact-first terminology while preserving the interaction or contract behavior. |
| 125 | `react-editor.test.tsx:2262` — double-click inserts a vertex while right-click selects it and waits for an explicit Delete action | `restored-editor.test.tsx:2413` — double-click inserts a vertex while right-click selects it and waits for an explicit Delete action | Path/pointer | Same behavior, ported to native schema-v3 document fixtures and assertions. |
| 126 | `react-editor.test.tsx:2287` — double-click adds a smooth point at the clicked location on a bendable cubic | `restored-editor.test.tsx:2438` — double-click adds a smooth point at the clicked location on a bendable cubic | Path/pointer | Same behavior, ported to native schema-v3 document fixtures and assertions. |
| 127 | `react-editor.test.tsx:2304` — vertex menu rounds a corner as a persisted quadratic | `restored-editor.test.tsx:2452` — vertex menu rounds a corner as a persisted quadratic | Path/pointer | Same behavior, ported to native schema-v3 document fixtures and assertions. |
| 128 | `react-editor.test.tsx:2318` — straight-segment menu converts a segment to a bendable curve with controls at both endpoints | `restored-editor.test.tsx:2466` — straight-segment menu converts a segment to a bendable curve with controls at both endpoints | Path/pointer | Same behavior, ported to native schema-v3 document fixtures and assertions. |
| 129 | `react-editor.test.tsx:2341` — the second cubic control stays independently draggable after Make bendable | `restored-editor.test.tsx:2489` — the second cubic control stays independently draggable after Make bendable | Path/pointer | Same behavior, ported to native schema-v3 document fixtures and assertions. |
| 130 | `react-editor.test.tsx:2365` — dragging a cubic made bendable by its menu action pulls its midpoint to the pointer | `restored-editor.test.tsx:2513` — dragging a cubic made bendable by its menu action pulls its midpoint to the pointer | Path/pointer | Same behavior, ported to native schema-v3 document fixtures and assertions. |
| 131 | `react-editor.test.tsx:2381` — a saved bendable segment reloads as pullable with unchanged endpoint anchors | `restored-editor.test.tsx:2527` — a saved bendable segment reloads as pullable with unchanged endpoint anchors | Persistence/client | Same behavior, ported to native schema-v3 document fixtures and assertions. |
| 132 | `react-editor.test.tsx:2419` — splitting a marked cubic leaves either descendant pullable | `restored-editor.test.tsx:2563` — splitting a marked cubic leaves either descendant pullable | Path/pointer | Same behavior, ported to native schema-v3 document fixtures and assertions. |
| 133 | `react-editor.test.tsx:2460` — a bendable segment remains opt-in after undo and redo | `restored-editor.test.tsx:2601` — a bendable segment remains opt-in after undo and redo | History/replacement | Same behavior, ported to native schema-v3 document fixtures and assertions. |
| 134 | `react-editor.test.tsx:2480` — dragging a constrained cubic moves its whole path instead of bending it | `restored-editor.test.tsx:2619` — dragging a constrained cubic moves its whole path instead of bending it | Path/pointer | Same behavior, ported to native schema-v3 document fixtures and assertions. |
| 135 | `react-editor.test.tsx:2501` — dragging an imported custom cubic moves its whole path instead of bending it | `restored-editor.test.tsx:2637` — dragging an imported custom cubic moves its whole path instead of bending it | Path/pointer | Same behavior, ported to native schema-v3 document fixtures and assertions. |
| 136 | `react-editor.test.tsx:2515` — controls stay uniquely addressable after converting two segments to bendable curves | `restored-editor.test.tsx:2652` — controls stay uniquely addressable after converting two segments to bendable curves | Path/pointer | Same behavior, ported to native schema-v3 document fixtures and assertions. |
| 137 | `react-editor.test.tsx:2552` — line menu snaps a diagonal custom-outline segment to the chosen axis | `restored-editor.test.tsx:2689` — line menu snaps a diagonal custom-outline segment to the chosen axis | Guides | Same behavior, ported to native schema-v3 document fixtures and assertions. |
| 138 | `react-editor.test.tsx:2576` — curved-segment menu makes a quadratic segment straight and removes its control | `restored-editor.test.tsx:2714` — curved-segment menu makes a quadratic segment straight and removes its control | Path/pointer | Same behavior, ported to native schema-v3 document fixtures and assertions. |
| 139 | `react-editor.test.tsx:2592` — curved-segment menu presents adding a smooth point at the right-click location | `restored-editor.test.tsx:2730` — curved-segment menu presents adding a smooth point at the right-click location | Path/pointer | Same behavior, ported to native schema-v3 document fixtures and assertions. |
| 140 | `react-editor.test.tsx:2612` — a serialized and dragged quadratic inflection point remains removable | `restored-editor.test.tsx:2748` — a serialized and dragged quadratic inflection point remains removable | Path/pointer | Same behavior, ported to native schema-v3 document fixtures and assertions. |
| 141 | `react-editor.test.tsx:2638` — straight-segment context menu chooses the closest eligible edge | `restored-editor.test.tsx:2775` — straight-segment context menu chooses the closest eligible edge | Path/pointer | Same behavior, ported to native schema-v3 document fixtures and assertions. |
| 142 | `react-editor.test.tsx:2652` — changing holds closes a stale line menu before it can edit the new selection | `restored-editor.test.tsx:2789` — changing contacts closes a stale line menu before it can edit the new selection | Persistence/client | Renamed to contact-first terminology while preserving the interaction or contract behavior. |
| 143 | `react-editor.test.tsx:2672` — vertex menu arrow navigation moves between actions without editing the selected hold | `restored-editor.test.tsx:2812` — vertex menu arrow navigation moves between actions without editing the selected contact | Path/pointer | Renamed to contact-first terminology while preserving the interaction or contract behavior. |
| 144 | `react-editor.test.tsx:2698` — left pointer-down selects a vertex and secondary-button movement never starts a drag | `restored-editor.test.tsx:2838` — left pointer-down selects a vertex and secondary-button movement never starts a drag | Path/pointer | Same behavior, ported to native schema-v3 document fixtures and assertions. |
| 145 | `react-editor.test.tsx:2727` — left pointer-up preserves a valid vertex selection for keyboard deletion | `restored-editor.test.tsx:2867` — left pointer-up preserves a valid vertex selection for keyboard deletion | Path/pointer | Same behavior, ported to native schema-v3 document fixtures and assertions. |
| 146 | `react-editor.test.tsx:2747` — focusing another vertex selects it before keyboard deletion | `restored-editor.test.tsx:2887` — focusing another vertex selects it before keyboard deletion | Path/pointer | Same behavior, ported to native schema-v3 document fixtures and assertions. |
| 147 | `react-editor.test.tsx:2776` — vertex button Enter and Space activation select the targeted point | `restored-editor.test.tsx:2914` — vertex button Enter and Space activation select the targeted point | Path/pointer | Same behavior, ported to native schema-v3 document fixtures and assertions. |
| 148 | `react-editor.test.tsx:2789` — four-vertex start vertices expose an enabled Delete action and are removed | `restored-editor.test.tsx:2927` — four-vertex start vertices expose an enabled Delete action and are removed | Path/pointer | Same behavior, ported to native schema-v3 document fixtures and assertions. |
| 149 | `react-editor.test.tsx:2800` — minimum-contour vertices expose a disabled Delete action without dirtying | `restored-editor.test.tsx:2938` — minimum-contour vertices expose a disabled Delete action without dirtying | Persistence/client | Same behavior, ported to native schema-v3 document fixtures and assertions. |
| 150 | `react-editor.test.tsx:2811` — Delete and Backspace remove only the selected eligible vertex and ignore form targets | `restored-editor.test.tsx:2949` — Delete and Backspace remove only the selected eligible vertex and ignore form targets | Path/pointer | Same behavior, ported to native schema-v3 document fixtures and assertions. |
| 151 | `react-editor.test.tsx:2847` — Escape returns focus to the selected vertex and outside pointer-down preserves target focus | `restored-editor.test.tsx:2983` — Escape returns focus to the selected vertex and outside pointer-down preserves target focus | Path/pointer | Same behavior, ported to native schema-v3 document fixtures and assertions. |
| 152 | `react-editor.test.tsx:2871` — the vertex menu measures and flips within the viewport edges | `restored-editor.test.tsx:3007` — the vertex menu measures and flips within the viewport edges | Path/pointer | Same behavior, ported to native schema-v3 document fixtures and assertions. |
| 153 | `react-editor.test.tsx:2911` — double-click insertion preserves prior status while clearing validation and marking dirty | `restored-editor.test.tsx:3047` — double-click insertion preserves prior status while clearing validation and marking dirty | Path/pointer | Same behavior, ported to native schema-v3 document fixtures and assertions. |
| 154 | `react-editor.test.tsx:2931` — menu deletion preserves prior status while clearing validation and marking dirty | `restored-editor.test.tsx:3067` — menu deletion preserves prior status while clearing validation and marking dirty | Path/pointer | Same behavior, ported to native schema-v3 document fixtures and assertions. |
| 155 | `react-editor.test.tsx:2952` — failed menu deletion preserves the selected vertex and menu while exposing validation | `restored-editor.test.tsx:3088` — failed menu deletion preserves the selected vertex and menu while exposing validation | Path/pointer | Same behavior, ported to native schema-v3 document fixtures and assertions. |
| 156 | `react-editor.test.tsx:2980` — replacing the document cancels an active gesture without letting later pointer events clobber it | `restored-editor.test.tsx:3114` — replacing the document cancels an active gesture without letting later pointer events clobber it | Path/pointer | Same behavior, ported to native schema-v3 document fixtures and assertions. |
| 157 | `react-editor.test.tsx:3018` — pointer cancellation, lost capture, and a second pointer preserve the initiating snapshot | `restored-editor.test.tsx:3153` — pointer cancellation, lost capture, and a second pointer preserve the initiating snapshot | Path/pointer | Same behavior, ported to native schema-v3 document fixtures and assertions. |
| 158 | `react-editor.test.tsx:3040` — invalid pointer geometry rolls back the path and dirty state | `restored-editor.test.tsx:3175` — invalid pointer geometry rolls back the path and dirty state | Path/pointer | Same behavior, ported to native schema-v3 document fixtures and assertions. |
| 159 | `react-editor.test.tsx:3055` — malformed selected paths report interaction failures instead of throwing | `restored-editor.test.tsx:3190` — malformed selected paths report interaction failures instead of throwing | Path/pointer | Same behavior, ported to native schema-v3 document fixtures and assertions. |
| 160 | `react-editor.test.tsx:3117` — outline picker reflects persisted constraints and changes every piece of the selected physical hold | `restored-editor.test.tsx:3238` — outline picker reflects persisted constraints and changes every piece of the selected physical contact | Constraints | Renamed to contact-first terminology while preserving the interaction or contract behavior. |
| 161 | `react-editor.test.tsx:3144` — primitive selection saves an exact zero-degree constraint and Custom removes only metadata | `restored-editor.test.tsx:3270` — primitive selection saves an exact zero-degree constraint and Custom removes only metadata | Constraints | Same behavior, ported to native schema-v3 document fixtures and assertions. |
| 162 | `react-editor.test.tsx:3167` — invalid outline actions preserve geometry while save-in-progress edits remain unsaved | `restored-editor.test.tsx:3293` — invalid outline actions preserve geometry while save-in-progress edits remain unsaved | Constraints | Same behavior, ported to native schema-v3 document fixtures and assertions. |
| 163 | `react-editor.test.tsx:3214` — constrained selections render an oriented box and eight handles instead of freeform controls | `restored-editor.test.tsx:3340` — constrained selections render an oriented box and eight handles instead of freeform controls | Constraints | Same behavior, ported to native schema-v3 document fixtures and assertions. |
| 164 | `react-editor.test.tsx:3235` — constrained resize keeps circles circular, isolates siblings, and allows finite off-canvas endpoints | `restored-editor.test.tsx:3361` — constrained resize keeps circles circular, isolates siblings, and allows finite off-canvas endpoints | Constraints | Same behavior, ported to native schema-v3 document fixtures and assertions. |
| 165 | `react-editor.test.tsx:3255` — constrained resize calculation failures immediately restore pointer-down geometry | `restored-editor.test.tsx:3381` — constrained resize calculation failures immediately restore pointer-down geometry | Constraints | Same behavior, ported to native schema-v3 document fixtures and assertions. |
| 166 | `react-editor.test.tsx:3272` — starting save during constrained resize rolls the gesture back before serializing | `restored-editor.test.tsx:3398` — starting save during constrained resize rolls the gesture back before serializing | Constraints | Same behavior, ported to native schema-v3 document fixtures and assertions. |
| 167 | `react-editor.test.tsx:3302` — constrained movement preserves metadata while every rotation mode updates sibling angles | `restored-editor.test.tsx:3428` — constrained movement preserves metadata while every rotation mode updates sibling angles | Constraints | Same behavior, ported to native schema-v3 document fixtures and assertions. |
| 168 | `react-editor.test.tsx:3336` — constrained pointer cancellation restores paths and angles | `restored-editor.test.tsx:3462` — constrained pointer cancellation restores paths and angles | Constraints | Same behavior, ported to native schema-v3 document fixtures and assertions. |
| 169 | `workbench-modules.test.ts:78` — the browser client lists and opens direct boards | `restored-modules.test.ts:131` — the browser client lists and opens direct boards | Persistence/client | Same behavior, ported to native schema-v3 document fixtures and assertions. |
| 170 | `workbench-modules.test.ts:112` — the browser client requests and validates a selected presentation | `restored-modules.test.ts:165` — the browser client requests and validates a selected presentation | Persistence/client | Same behavior, ported to native schema-v3 document fixtures and assertions. |
| 171 | `workbench-modules.test.ts:157` — the browser client deletes a selected presentation and returns the next focused board | `restored-modules.test.ts:207` — the browser client deletes a selected presentation and returns the next focused board | Persistence/client | Same behavior, ported to native schema-v3 document fixtures and assertions. |
| 172 | `workbench-modules.test.ts:190` — backend requests carry a fifteen-second timeout signal | `restored-modules.test.ts:240` — backend requests carry a fifteen-second timeout signal | Persistence/client | Same behavior, ported to native schema-v3 document fixtures and assertions. |
| 173 | `workbench-modules.test.ts:206` — the browser entry module can be imported without a document | `restored-modules.test.ts:256` — the browser entry module can be imported without a document | App/editor integration | Same behavior, ported to native schema-v3 document fixtures and assertions. |
| 174 | `workbench-modules.test.ts:211` — the browser client rejects invalid optional board URLs | `restored-modules.test.ts:261` — the browser client rejects invalid optional board URLs | Persistence/client | Same behavior, ported to native schema-v3 document fixtures and assertions. |
| 175 | `workbench-modules.test.ts:242` — the browser client rejects invalid optional hold-region fields | `restored-modules.test.ts:292` — the browser client rejects invalid optional contact-region fields | Persistence/client | Renamed to contact-first terminology while preserving the interaction or contract behavior. |
| 176 | `workbench-modules.test.ts:304` — the browser client preserves a fractional fixed depth | `restored-modules.test.ts:317` — the browser client preserves an equal-bound factual depth range | Persistence/client | Scalar region depth retired; replaced by contact-owned equal-bound/ranged `depthRangeMillimeters` validation. |
| 177 | `workbench-modules.test.ts:321` — the browser client preserves valid sloper metadata | `restored-modules.test.ts:335` — the browser client preserves valid contact kind and raster treatment metadata | Persistence/client | Region-owned sloper facts retired; replaced by contact kind plus raster `treatment` ownership. |
| 178 | `workbench-modules.test.ts:342` — the browser client rejects invalid sloper metadata | `restored-modules.test.ts:353` — the browser client rejects invalid raster treatment metadata | Persistence/client | Region-owned sloper facts retired; replaced by contact kind plus raster `treatment` ownership. |
| 179 | `workbench-modules.test.ts:375` — the browser client rejects invalid or ambiguous fixed-depth payloads | `restored-modules.test.ts:376` — the browser client rejects invalid contact depth-range payloads | Persistence/client | Renamed to contact-first terminology while preserving the interaction or contract behavior. |
| 180 | `workbench-modules.test.ts:462` — the browser client navigates to login when an API request is unauthenticated | `restored-modules.test.ts:402` — the browser client navigates to login when an API request is unauthenticated | Git/auth | Same behavior, ported to native schema-v3 document fixtures and assertions. |
| 181 | `workbench-modules.test.ts:474` — the browser client preserves an auth-status API failure | `restored-modules.test.ts:414` — the browser client preserves an auth-status API failure | Git/auth | Same behavior, ported to native schema-v3 document fixtures and assertions. |
| 182 | `workbench-modules.test.ts:486` — the browser client keeps the current tab on an unauthenticated save and exposes the login URL | `restored-modules.test.ts:426` — the browser client keeps the current tab on an unauthenticated save and exposes the login URL | Git/auth | Same behavior, ported to native schema-v3 document fixtures and assertions. |
| 183 | `workbench-modules.test.ts:512` — the browser client saves one direct editor document with PUT | `restored-modules.test.ts:452` — the browser client saves one direct editor document with PUT | Persistence/client | Same behavior, ported to native schema-v3 document fixtures and assertions. |
| 184 | `workbench-modules.test.ts:528` — the browser client can read git status and run git operations | `restored-modules.test.ts:468` — the browser client can read git status and run git operations | Git/auth | Same behavior, ported to native schema-v3 document fixtures and assertions. |
| 185 | `workbench-modules.test.ts:586` — getGitStatus falls back to an empty statusLines array for null or non-array values | `restored-modules.test.ts:526` — getGitStatus falls back to an empty statusLines array for null or non-array values | Git/auth | Same behavior, ported to native schema-v3 document fixtures and assertions. |
| 186 | `workbench-modules.test.ts:620` — native diagnostic failures do not replace the useful request error | `restored-modules.test.ts:560` — native diagnostic failures do not replace the useful request error | Persistence/client | Same behavior, ported to native schema-v3 document fixtures and assertions. |
| 187 | `workbench-modules.test.ts:635` — native diagnostics ignore malformed browser bridge shapes | `restored-modules.test.ts:575` — native diagnostics ignore malformed browser bridge shapes | App/editor integration | Same behavior, ported to native schema-v3 document fixtures and assertions. |
| 188 | `workbench-modules.test.ts:648` — direct board loading commits image and holds together and preserves the prior editor on failure | `restored-modules.test.ts:588` — direct board loading commits image and contacts together and preserves the prior editor on failure | Persistence/client | Renamed to contact-first terminology while preserving the interaction or contract behavior. |
| 189 | `workbench-modules.test.ts:687` — direct board loading uses a matching preloaded image promise | `restored-modules.test.ts:628` — direct board loading uses a matching preloaded image promise | Persistence/client | Same behavior, ported to native schema-v3 document fixtures and assertions. |
| 190 | `workbench-modules.test.ts:706` — direct board loading ignores a preloaded image from a different URL | `restored-modules.test.ts:648` — direct board loading ignores a preloaded image from a different URL | Persistence/client | Same behavior, ported to native schema-v3 document fixtures and assertions. |
| 191 | `workbench-modules.test.ts:728` — direct board loading rejects malformed shape constraints before image loading or commit | `restored-modules.test.ts:671` — direct board loading rejects malformed shape constraints before image loading or commit | Constraints | Same behavior, ported to native schema-v3 document fixtures and assertions. |
| 192 | `workbench-modules.test.ts:773` — the direct editor model rejects duplicate and open hold paths before saving | `restored-modules.test.ts:694` — the direct editor model rejects duplicate and open contact paths before saving | Path/pointer | Renamed to contact-first terminology while preserving the interaction or contract behavior. |
| 193 | `workbench-modules.test.ts:784` — the direct editor model rejects the removed schema version field | `restored-modules.test.ts:708` — the direct editor model rejects the removed schema version field | Presentation/model | Same behavior, ported to native schema-v3 document fixtures and assertions. |
| 194 | `workbench-modules.test.ts:795` — the direct editor model rejects invalid optional hold-region fields | `restored-modules.test.ts:712` — the direct editor model rejects invalid optional contact-region fields | Contact facts/media | Renamed to contact-first terminology while preserving the interaction or contract behavior. |
| 195 | `workbench-modules.test.ts:828` — the direct editor model rejects malformed gaston pair identifiers before saving | `restored-modules.test.ts:727` — the direct editor model rejects malformed gaston pair identifiers before saving | Contact facts/media | Same behavior, ported to native schema-v3 document fixtures and assertions. |
| 196 | `workbench-modules.test.ts:850` — the editor document clones and validates bendable curve command indexes | `restored-modules.test.ts:733` — the editor document clones and validates bendable curve command indexes | Path/pointer | Same behavior, ported to native schema-v3 document fixtures and assertions. |
| 197 | `workbench-modules.test.ts:875` — the direct editor model validates and deeply clones sloper metadata | `restored-modules.test.ts:752` — the direct editor model validates and deeply clones raster treatment metadata | Contact facts/media | Region-owned sloper facts retired; replaced by contact kind plus raster `treatment` ownership. |
| 198 | `workbench-modules.test.ts:896` — the direct editor model rejects invalid and inconsistent sloper metadata | `restored-modules.test.ts:765` — the direct editor model rejects invalid raster treatment metadata | Contact facts/media | Region-owned sloper facts retired; replaced by contact kind plus raster `treatment` ownership. |
| 199 | `workbench-modules.test.ts:938` — the direct editor model rejects inconsistent finger capacities for one physical hold | `restored-modules.test.ts:778` — the direct editor model rejects invalid factual finger capacities | Contact facts/media | Duplicated per-region facts retired; replaced by one factual contact shared by all media pieces. |
| 200 | `workbench-modules.test.ts:961` — the direct editor model accepts positive finite fractional depth ranges | `restored-modules.test.ts:786` — the direct editor model accepts positive finite fractional depth ranges | Contact facts/media | Same behavior, ported to native schema-v3 document fixtures and assertions. |
| 201 | `workbench-modules.test.ts:973` — the direct editor model accepts positive finite fractional fixed depth | `restored-modules.test.ts:792` — the direct editor model accepts equal bounds for source-backed fixed depth | Contact facts/media | Scalar region depth retired; replaced by contact-owned equal-bound/ranged `depthRangeMillimeters` validation. |
| 202 | `workbench-modules.test.ts:985` — the direct editor model rejects invalid or ambiguous fixed depth | `restored-modules.test.ts:798` — the direct editor model rejects nonpositive contact depth-range values | Contact facts/media | Scalar per-region depth retired; replaced by rejection of nonpositive bounds on contact-owned `depthRangeMillimeters`. |
| 203 | `workbench-modules.test.ts:1009` — the direct editor model rejects inconsistent sibling fixed-depth representation | `restored-modules.test.ts:824` — contact facts have one owner regardless of how many media pieces reference them | Contact facts/media | Per-region factual depth conflicts retired; replaced by one explicit factual contact shared by every raster piece, with no first-wins projection. |
| 204 | `workbench-modules.test.ts:1035` — the direct editor model rejects malformed, non-finite, and unordered depth ranges | `restored-modules.test.ts:809` — the direct editor model rejects malformed, non-finite, missing, and reversed contact depth ranges | Contact facts/media | Native `contacts[0].depthRangeMillimeters` rejects `{}`, each missing bound, representable non-finite bounds, and reversed bounds. |
| 205 | `workbench-modules.test.ts:1058` — the direct editor model rejects invalid and inconsistent hand capacities | `restored-modules.test.ts:846` — the direct editor model rejects invalid factual hand capacities | Contact facts/media | Duplicated per-region facts retired; replaced by one factual contact shared by all media pieces. |
| 206 | `workbench-modules.test.ts:1094` — a rejected save keeps the editor document untouched | `restored-modules.test.ts:854` — a rejected save keeps the editor document untouched | Persistence/client | Same behavior, ported to native schema-v3 document fixtures and assertions. |
| 207 | `workbench-modules.test.ts:1112` — board operations serialize out-of-order selections | `restored-modules.test.ts:873` — board operations serialize out-of-order selections | Persistence/client | Same behavior, ported to native schema-v3 document fixtures and assertions. |
| 208 | `workbench-modules.test.ts:1137` — a pending save cannot overwrite a board selected after the operation changes | `restored-modules.test.ts:898` — a pending save cannot overwrite a board selected after the operation changes | Persistence/client | Same behavior, ported to native schema-v3 document fixtures and assertions. |
