# Task 1 brief — media-aware logical matching

Implement only Task 1 from the approved package-driven runtime plan.

- Resolve frames from selected raster union geometry or a model descriptor AABB.
- `TrainingBoard.holds(in:)` selects only holds resolvable in that presentation.
- Board maps/workout matching do not borrow a different presentation's geometry.
- Preserve raster behavior and Compact II `sloper-round-center` substitution.
- Never copy descriptor bounds into logical JSON.
